# ML Training
# Offline egitim scriptleri bu dizindedir.
# Canli skorlama warmup+freeze ile `domain/ml_detectors.py` icindedir (joblib sart degil).
# Set 2 IF nicelik taramasi: `ml/notebooks/ims_set2_ml_calibration.md` (ADR-0008).
